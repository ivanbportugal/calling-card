{
  "name": "backend",
  "version": "1.0.0",
  "description": "",
  "main": "src/server.ts",
  "scripts": {
    "dev": "node --watch src/server.ts",
    "start": "node src/server.ts",
    "typecheck": "tsc --noEmit",
    "prisma:generate": "prisma generate",
    "prisma:migrate": "prisma migrate dev",
    "postinstall": "prisma generate",
    "vercel-build": "cd ../marketing && pnpm install && pnpm generate-routes && pnpm build && cd ../backend && rm -rf public && mkdir -p public && cp -r ../marketing/dist/* public/ && prisma generate && prisma migrate deploy",
    "pretest": "prisma generate",
    "test": "node --test test/**/*.test.ts"
  },
  "keywords": [],
  "author": "",
  "license": "ISC",
  "packageManager": "pnpm@11.20.0",
  "type": "module",
  "dependencies": {
    "@fastify/static": "^10.1.3",
    "@prisma/adapter-pg": "^7.9.1",
    "@prisma/client": "^7.9.1",
    "dotenv": "^17.4.2",
    "fastify": "^5.11.2",
    "fastify-plugin": "^6.0.0",
    "firebase-admin": "^14.2.0",
    "jwks-rsa": "^4.1.0",
    "pg": "^8.22.0"
  },
  "devDependencies": {
    "@testcontainers/postgresql": "^12.1.0",
    "@types/node": "^24.0.0",
    "pino-pretty": "^13.1.3",
    "prisma": "^7.9.1",
    "testcontainers": "^12.1.0",
    "typescript": "^5.7.0"
  }
}
