DATABASE_URL="postgresql://user:password@localhost:5432/calling_card?schema=public"

# Path to Firebase service account JSON, or set FIREBASE_SERVICE_ACCOUNT_JSON with the raw JSON content
GOOGLE_APPLICATION_CREDENTIALS="./firebase-service-account.json"

PORT=3000
